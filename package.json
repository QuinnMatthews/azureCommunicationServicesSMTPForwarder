{
  "name": "azurecommunicationservicessmtpforwarder",
  "version": "1.0.0",
  "description": "Accepts SMTP requests and converts them to requests for azure communication services",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Quinn Matthews",
  "license": "GPL-3.0",
  "dependencies": {
    "@azure/communication-email": "^1.0.0-beta.1",
    "dotenv": "^16.0.3",
    "mailparser": "^3.6.3",
    "smtp-server": "^3.11.0"
  }
}
